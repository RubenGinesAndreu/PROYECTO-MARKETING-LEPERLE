{
  "name": "le-perle-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site - no build needed'",
    "start": "npx serve public -l 3000"
  }
}
